# 2026-04-07

- Created: 2026-04-07
- Updated: 2026-04-07
- Type: journal
- Status: draft
- Tags: learning, agents, nanobot, langgraph, streaming, session
- Model: GPT-5.4
- Harness: Codex
- Source: daily work and learning session

## Focus

今天主要推进了两条线：

- 系统学习 LLM 流式输出与 LangGraph 的流式机制，并把它整理成一篇更像教程的学习笔记
- 回到 `nanobot` 主线，重新澄清“真正值得学的部分”是什么，避免把精力花在不重要的入口层

## Tracks

- agents
- learning
- job-hunt

## Inputs

今天主要看了这些内容：

- OpenAI、Anthropic、Hugging Face、vLLM 关于流式输出、SSE、KV cache、推理阶段性能的官方资料
- LangGraph 关于 `messages / updates / custom` 的 streaming 文档与相关实现思路
- `nanobot` 的整体结构，以及和主 agent / 中控 / 队列 / 上下文管理相关的核心文件边界
- 仓库里现有的 `knowledge-note-template`、学习笔记内容和模板约定

## Worklog

- 系统梳理了 LLM 流式输出的基础概念：`token`、`prefill`、`decode`、`TTFT`、`ITL`、`KV cache`、`SSE`
- 梳理了 LangGraph 的流式输出层次，重点区分：
  - `messages`：LLM token 流
  - `updates`：状态更新
  - `custom`：自定义事件流
- 形成并多轮重写了 `learning/llm-streaming-and-langgraph-streaming.md`
- 特意把学习笔记的开头改成“模型层 / 传输层 / 框架层”的三层引导，并补上更清楚的基础承接说明
- 新增了 `templates/learning-note-template.md`，把“教程式学习笔记”的结构单独抽出来
- 新增 `templates/README.md`，明确 `knowledge-note-template` 与 `learning-note-template` 的区别，以及模板选择规则
- 在 `AGENTS.md` 中补上模板选择规则，帮助后续 agent 自动判断新增条目应使用哪个模板
- 回到 `nanobot` 学习主线，重新判断：
  - `gateway()` 不是当前最值得深读的重点
  - 真正更贴近实习项目第一点需求的，是 `loop.py`、`runner.py`、`queue.py`、`context.py`、`session/manager.py`
- 总结了 `nanobot` 对当前目标真正有帮助的三个学习方向：
  - 主 agent 如何持续推进任务直到完成
  - 不同消息/会话如何做串行隔离与并发处理
  - 主中控如何组装和维护上下文

## Outputs

今天形成了这些可复用结果：

- 一篇关于流式输出与 LangGraph streaming 的学习笔记
- 一个更适合教程式内容的学习笔记模板
- 一套模板选择规则
- 一版更聚焦当前目标的 `nanobot` 学习路线判断

## Promotions

今天有哪些内容值得晋升为长期资产？

- promote to learning: `learning/llm-streaming-and-langgraph-streaming.md`
- promote to solution: 暂无
- promote to playbook: 暂无
- promote to skill: 暂无

## Frictions

- `nanobot` 目前只完成了结构澄清和阅读路线重定向，还没有真正进入 `loop.py` 的源码精读
- 对“主 agent 中控 / 中心任务调度”这类问题，`nanobot` 提供的是轻量实现，不是强显式 planner 或复杂任务树系统

## Insights

- 学习笔记要真正好读，关键不是省字数，而是先把概念拆层，再把承接关系讲清楚
- 对 agent 项目的学习顺序，应该跟自己的目标对齐，而不是被入口函数或文件名牵着走
- 对你当前的实习项目和简历目标来说，`nanobot` 最值得学的是轻量中控循环、会话隔离与上下文管理，而不是外围启动装配

## Time Estimate

- Deep Work Blocks: 3
- Hours Est: 4-5

## Token Estimate

- Sessions: 4
- Tokens Est: not tracked
- Notes: 今天主要在概念梳理、学习笔记重写与源码学习路线校正之间切换

## Next Actions

- 下一步直接开始读 `nanobot/agent/loop.py`，重点看 `run()`、`_dispatch()` 与 `process_direct()`
- 然后继续读 `runner.py`，重点看主循环如何通过 tool-calling 持续推进任务
- 等这两部分理解更扎实后，再回头重写简历里平安科技实习的第 1 点和第 4 点
